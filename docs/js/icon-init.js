document.addEventListener("DOMContentLoaded", () => {
  if (window.lucide) {
    lucide.createIcons({
      attrs: {
        width: 50,
        height: 50,
        stroke: "#2094f3", // Dhanman primary color
        "stroke-width": .8,
      },
    });
  }
});
